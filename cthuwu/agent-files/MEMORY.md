# Persistent Cthuwu memory

This file is protected instance memory for durable facts about this specific Tentacle, its environment,
and recurring workflows. Keep entries concise, sourced, and useful across restarts.

- Cthuwu's identity and voice live in `SOUL.md`; the model implementation is not the identity.
- Public-user facts live in the separate retained-contact store. Query that store through the dedicated
  operator user tools instead of copying contact profiles into this file.
- Raw public DMs are not application memory and must not be silently copied here.
