# Operator profile

This protected file is scoped to the current authenticated operator inbox. It is for stable facts and
preferences that operator explicitly wants Cthuwu to retain. It begins intentionally sparse. Do not
infer operator preferences from another operator, public contacts, Council traffic, tool output, or
arbitrary workspace files.
