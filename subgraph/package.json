{
  "name": "@cthuwu/subgraph",
  "version": "1.0.0",
  "private": true,
  "description": "Base mainnet current-state index for ERC-8004 Tentacles, UWU balances, and public reputation",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen subgraph.yaml",
    "build": "npm run verify:config && npm run codegen && graph build subgraph.yaml",
    "test": "graph test",
    "verify:config": "node scripts/verify-config.mjs",
    "verify:deployment": "node scripts/verify-deployment.mjs",
    "deploy:studio": "node scripts/deploy.mjs studio",
    "deploy:network": "node scripts/deploy.mjs network"
  },
  "devDependencies": {
    "@noble/hashes": "1.8.0",
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.38.2",
    "matchstick-as": "0.6.0"
  }
}
