{
  "name": "@cthuwu/xmtp-sidecar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Official XMTP Agent SDK transport sidecar for uwubot",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@xmtp/agent-sdk": "2.3.0",
    "viem": "2.55.10"
  },
  "devDependencies": {
    "@types/node": "24.1.0",
    "typescript": "5.8.3",
    "vitest": "3.2.7"
  }
}
