# XMTP dev release-gate run — 2026-08-01

## Scope

- Base commit: `0f6b1e845bdd7e4449d265d6a3641030be22ebf2`
- XMTP environment: `dev`
- Bot public address: `0x52a93ca2cf0629bcfe7bf7824df7c18268c360f7`
- Clean-browser public address: `0x7d86f7947542c35beb8160ef77b3502a6bf8ebc3`
- Unique marker: `CTHUWU-LIVE-20260801-1835-7F2A`
- Node: `22.22.2`
- Rust: `1.97.0`
- Configured Pages run: <https://github.com/pierce403/cthuwu/actions/runs/30727246650>

No inbox ID, private key, database key, model credential, contact-note body, or XMTP database
content was recorded.

## Evidence

- A clean browser load created a random local identity without a wallet request. The same public
  address appeared after reload and after backend restart.
- The deployed site connected to the configured bot on `dev`.
- The unique marker produced one outbound browser message and exactly one Cthuwu reply.
- The first message created exactly `contacts/<lowercase-browser-inbox-id>.md` with mode `0600`.
- Harmless onboarding answers for name, hopes, offered resources, needed resources, and explicit
  sharing consent were matched exactly without printing the note. Markdown heading syntax remained
  blockquoted and could not create note structure.
- `/profile`, `/set`, `/share off`, `/share on`, `/pause`, `/resume`, `/matches`, and
  `/forget confirm` each produced exactly one reply and the expected state transition.
- A synthetic comparison contact was invisible while opted out. After bilateral opt-in, the match
  exposed only its chosen name and overlapping matching terms, labeled the result suggestion-only,
  and omitted the synthetic inbox ID and unrelated profile text.
- Graceful SIGINT exited successfully. Multiple restarts reused the same bot public address and the
  unchanged owner-only identity file. Browser reload restored 16 sent and 16 received messages,
  the corrected profile, and the ongoing conversation.
- Replay tombstones remained hashed, empty, and mode `0600` across restarts. The initial live marker
  never produced a duplicate reply.
- Both generated contact notes were removed through their respective `/forget confirm` commands.
  Persistent identity/database state and replay tombstones remain outside the repository.
- Normal `uwubot` diagnostics contained no keys, credentials, or message bodies.

## Verification result

Local web, sidecar, Rust, npm-audit, Docker, browser, live XMTP, restart, consent, matching, and
deletion checks passed. The release criterion requiring a real XMTP end-to-end test to run in
GitHub CI remains unchecked because the workflow currently has no live XMTP/browser job; this
manual run is direct evidence but is not CI evidence.
